# Merge `junit.xml` files from pytest into one

```sh
$ ./junitmerge.py --help
usage: junitmerge.py [-h] input [input ...] output

positional arguments:
  input
  output

options:
  -h, --help  show this help message and exit
```
